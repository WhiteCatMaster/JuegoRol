import { Routes } from '@angular/router';
import { CreadorHechizos } from './creador-hechizos/creador-hechizos';

export const routes: Routes = [
    { path: 'creador', component: CreadorHechizos }
];
